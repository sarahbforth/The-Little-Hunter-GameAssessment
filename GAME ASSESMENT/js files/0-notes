LOG
------

Structure plan: Title - Cutscene - Explore & Collect - Village & NPCs - Boss - Final Scene

System Requirements: Scene managers/Player system/World system/Interaction system/Dialogue/Combat/UI system/Game state machine

------

Intro cutscene: Introduce character and explain goal
                Must have background image, show dialogue box, press key to advance
Explore scene: Player movement/animations and basic exploration of a location to collect loot
                Must have movement controls, object collisions (loot and walls), money count increase, exit unlocks/triggers after goal is reached.
Village scene: NPC interactions for storyline progress with potential item trades
                Must have NPC interction control, dialogue controls, quest triggers.
Boss Arena/Combat: The final boss for questline (only combat scene in game)
                Must have one boss fully animated, battle UI systems, attack/damage mechanics, trigger win/lose scene.

-----

Potential classes - Player/NPC/Enemy/DialogueBoxes/Items

Keyboard input for movement/collision triggers/scene changes

Algorithms - collision detection/enemy attack and movement/dialogue progressions

-----

Multilpe files for structure? External libraries if needed?  
- assets
- json data (sprite animations)
- sketch (preload)
- global variables?
- character info
- scene info
- game state managers?
- game ui info?

//REMINDER must alter html for extra files//

-----

Potential Class Hierarchy

(Use superclasses for easy expansion?)
Game characters:  ENTITY > CHARACTER > PLAYER > NPC > ENEMY
Items: ITEM > LOOT
Game Scenes: BASE SCENE > TITLE SCENE > CUTSCENE > EXPLORE > HUB/VILLAGE > BATTLE > END SCENE
Game Managers: MAIN MANAGER > SCENE MANAGER

-----

Buiding a scene manager - must have one active scene at a time, store scene information, relative methods (eg. enter.display)

Custom function ideas = addScene / changeScene / display / keyPressed or mousePressed

Game manager to hold info and possible replay runs = have multiple properties to check game progress.
- activeQuest / questComplete / gameOver / gameWim / save run numbers for replays?

-----

Building scene classes: Base super class (each scene will have the same structure, occational differences in properties)

Custom function ideas = enterScene / exitScene / display / keyPressed or mousePressed (use scene manager to change scenes?)

-----

Implement everything in sketch and test.

Added drawSceneLabel custom function to build skeleton and check the managers and key handlers work (basic text and labels on screen) - encountered a bug, just a typo.

-----

Building character classes: Using same logic as the scene class, one entity super class with basic properties and methods then expand from there.

Player properties: canvas position and size / name / health / movement and speed / canvas constraints.

- used basic rect() for visibility - keyIsDown() for smooth movement - enter in setup() - add display method to the scenes' display.

- player moves smoothly, no bugs.

-----

Testing loot collection in the explore scene:

Add money property to the player to keep track / create a function for it to add an amount.

Collision will be a big part of the game- create a generic collision function in a seperate file?

//AI USED FOR CONFIRMATION//

Item class: super class method is working - ITEM > LOOT (change in propeties)

- added a display method & collect function to add value to player.

- added a loot array to the explore scene to hold the loot objects and display

- added a player check allLootCollected - player can now only leave once all the objects are found.

-----

Player animation sprite sheet and json file -  taken from my own work (Week 7 - Practical 1) json template taken from same practical example.

- load assets in preload

- added new properties to player class = speed / animations array / frames

Animation methods in player class: loadAnimationFrames (for loop to iterate through the frame data and save in correct arrays)

- added display methods using new sprite sheet info

- changed some global variables to player specific properties

//AI USED FOR ANIMATION HELP//

"let index = floor(this.frame) % animation.length;"
- "...standard formula for looping through a sequence of frames in an animation"
- "...ensures that an animation can run indefinitely"
- "...allows you to control animation speed independent of frame rate"

- added direction property / used translate as learned from PDM1 assessment to change animation direction

-----

Research into fade transitions between scenes

W7_L2_FancyLevels

Transition system within scene manager? > fade out - switch scene - fade back in

Need variables to hold phases and scene change logic

- forceSceneChange and updateFade experiment - required a lot of changes and multiple errors (calling funtions in the wrong order/wrong place) but worked eventually

-----

New player sprite animation frames hand drawn and uploaded into assets.

- bug! can't push all frames into the same array, the program gets confused.

- created seperate animation arrays for "front", "side", "back" - build the arrays using the for loop structure in setup()
Track movement using player properties and keyIsDown functions.

-----

Dialogue box experiements

W7_L2_DialogueBoxTypewriter

Build class in systems as it is gamewide?
- built in UI file

- added text wrapping, nextLine functions and reset

Building dialogue box system into cutscenes - add to constructor?

- built an array of dialogue lines into the constructor and initiated in setup()

- added isFinishedLine helper (if line is still typing, space key completes the line instantly, or moves to next line)
implement in handleKeyPressed

- bug! dialogue starts during fade transition, need fade to complete before the dialogue.

-----

Environment creation for explore scene

W7_L1_Environment

Create an environment class and have custom functions for each tile.

//AI research for building a location with a tilemap and json file//

Requirements - a tilemap renderer, map layout array (2D), collision tiles, loot positions

Experimenting with class building and methods similar to frame animation for loop.

New syntax learned "let w = drawW ?? tile.w;" = "Use drawW if it exists, otherwise use the tile's normal width"

- bugs! misspelled commands, undefined functions - is this the best method..?

Pause for refelction - i'm getting a little overwhelmed with how to set out locations and levels. Do i use the sprite sheet to build it manually? do i photoshop a background for the environment - but then will the boundaries for the player be harder to program? we also learned how to do a centred camera with locked areas which would be a nice touch but i'm just unsure whats going to work better here...

- process change, manually built the explore scene using external assets in Adobe Illustrator, will implement it in the correct scene and centre the camera over the player. 

Save cameraX/Y as globals and draw the background relative to the world.

- if player moves right, camera mves right / if player moves down, camera moves down, player must appear centred.
"CameraX = player.x - width/2;"

- lock camera inside the environment, use canvas size and image size to contrain the movements (updateCamera)

- update animations and loot images using new camera variables

- additional imagery for details (manually work out coordinates of gem spawn points)

-----

Obstacles within an environment

Location maps are complex, is it better to build boundaries using multiple different rectangles?

- manually worked out central abstacle coordinates, build them as collision rectangles.

- create new helper to go along with rectCollision

- movePlayerWithCollisions created > idea = 

- bug! animations have stopped working, will need to find a fix
- bug! camera has stopped centralising the player

-----

Built an exit zone to complete the level, added a helper method and a level condition (all loot must be collected and player must return to the exit zone)

-----

Hub scene creation - same methods as the explore scene (thank you for classes!)

(Image built in Adobe Illustrator, implemented with the same method as before with boundaries - no bugs)

-----

NPC animation trials

Gathered external assets of sprite sheets and created seperate json files for all frame data using the same template as before.

Class created using similar methods as the player(update, animation, display etc)

- added globals, preloaded assets and files, extracted frames using same for loop method to fill the arrays

- bug! lots of framing issues with the animations and positioning

- added a draw offset so the draw placement is seperate to the animation frames

- learned "frame.name.startsWith" dot notation - helpful for filling seperate animation arrays

- successfully added multiple NPCs within the location

-----

Adding NPC interaction and dialogue

NPC properties now include an array of dialogue lines as a property

Must have - measure active state of dialogue / proximity check for player+NPC / keyPressed to activate

Stop player movement when in dialogue?

- no bugs, dialogue class allowed the build to be seamless once all other properties were definied

Extra feature - once player has spoken to the blacksmith, force scene and story progression?

- added condition using specifiic dialogue id and scene manager to add new cutscene

-----

Explore scene details

Map is empty and needs some kind of npc to fit the brief (static enemies / random creature to give instructions?)

- found external asset for static enemies, json file and sprite sheets loaded with the same method as before

- added an Enemy class with similar properties to npc to control animations and draw functions, also will have a damage function

As soon as there is collision detected, the player loses health every frame and dies immediately...

- added new properties to player - lastHitTime and hitCooldown as a buffer

- learned new function "millis()" = keeps track of how long the sketch has been running in milliseconds (p5.js reference website) helps measure time without counting the frames the sketch is running

//AI used to help with building functions using new properties//

- created new slime instances in the correct scene and used rect collision to register hit and take damage

- bug! using the update function is helpful but i keep forgetting to add the new game features in the scenes

- bug! player was taking damage too far away from the enemy - adjusted properties to fix it after resizing the sprite

-----

Taking damage from the enemy is printing in the console log but there is no feedback for the user.

Maybe add a flash to the player animation to indicate damage?

- added a hurtTimer property to the player so the effect only lasts for a certain amount of frames

Experimented with tint() to the frames (red/white/black colourings) - not super effective the colour is too subtle and blends with the sprite itself. Can I flicker the animation somehow?

Initial tests playing with frame count did nothing - //AI used for explanation and debugging//
- "let shouldDraw = true" / "shouldDraw = frameCount % 4 < 2"
- Explanation = initial experiments were checking conditions without storing or using the results.

-----

The hub/village has too much negative space, re-drew the map to add an animal pen and external animation frames found.

- animation arrays created, json files loaded with the same template and implemented in setup()

- created a basic class (similar to enemy to handle properties and methods) for a static animal, will expand to moving animal

//AI used for some explanation on how to get the program to choose movement randomly//
- "state machines = setState and pickNewState using random to pick direction and timings"

- bug! animations were placed off map by accident

- bug! more framing issues and movement range was set too small and sprite needed resizing

- json file was incorrect, now fixed

-----

Adding a new cutscene

- bug! background image was stuck to one specific image, changed a property in the class to take a new image every time

-----

- more details added to each location (more npcs, animations, dialogue, collisions)

-----

Final level - boss fight

One side-facing/platformer style level is allowed as I have followed the brief for all previous levels - easier to focus on combat mechanics rather than location and movement.

Ideal level style - one fixed background image, fixed camera no scrolling, set ground level (no obstacles or platforms)

-----

Building the battle scene class

- using standard scene base but added propety of set ground level for player and boss positions

- created placeholder characters using rect() to measure the scene

My original player class uses properties and animation styles that i wont need for this level - do i make a new one?

Must have: side-view arena, jump physics?, attacks, health, damages, correct animations

- created new battle player class to include new properties eg. speed, gravity and jump strength (will research more later), directions, attack state, cooldowns, hit timers, hurt timers

//AI used to explain how to implement jumping physics//
- "use vx/vy to measure the velocity of left/right & up/down - save as a variable for easier control"
- "vy can control vertical movement = jumping = negative value / falling = positive value"

When using these variables and then adding with a gravity variable, when a key is pressed the jump will start upwards, the upwards speed then slows down and the player falls back down

- new sprite data added to json files using existing sprite sheet, for loop in setup() to fill new animation arrays

- no bugs, animation workds perfectly when handled with key pressed.

-----

Creating a boss class

Can extend character - must have: speed, damage, attack state, direction, health, cool downs, hit timers

Use similar methods to player, must move independently within the contraints of the canvas and use cooldowns to work out timings

- built getAttackBox and takeDamage with visuals (rect()) to check attack timings and range

- learned abs() = "it turns a number into its distance from zero, ignoring whether its positive or negative"

Can i implement this to work out the boss distance from player and get the boss to move?

- added boss sprite sheet and json files (strange frame layout on the sheet, took forever), new frames to player json, same method to fill animation arrays.

- bug! boss sprite allignment with hit box and atatck box is way off, lots of adjustments needed

- bug! issue with code priority, attack state is ending too quickly and not resetting frames

- updated battle scene logic to initaite combat system using custom functions from the class, using animations depending on state

- bug! death animations work but the scene changes before the animatin finishes, added conditions so the deaths only trigger once / combat only initiates whilst both characters are alive / wait for death animtions to finish before changing scenes

- bug! undefined frames - typo!

- bug! didn't update the frames for the death animation so it never progressed

- bug! boss alignment once again, adjustments made

Fight mechanics tested, the fight unbalanced is totally unwinable by the player...

Possible fixes: increase player damage / adjust attack box positions / increase boss cooldown

-----

Additional intro cutscene & Winner/Game over cutscenes

New cutscenes made in Adobe Illustrator and endgame scene change depending on fight conditions

- bug! initialisation structure was wrong in setup()

- bug! text allignment issue, adjustments made

-----

Back to fight mechanics

Change in style - adjusted hit boxes for a much closer fit to the sprite to allow close combat / lowered boss attack boxes and range so they can be avoided correctly / player attack box changed to more AOE style to allow attacks from above

- bug! boss alignment issues again!

- printed registered hits and health stats to console log to check the program is working, fight feels more baanced.

- bug! the boss changes direction mid attack making it impossible to dodge - need to lock attack direction once its decided

No user feedback when damage is taken - need to use the same frame flashing as before

-----

Final details

Explore scene - hole must be dangerous, player respawns in the entry and takes damage

- added a respawn method and collision check with holeZone (similar to exitZone method)

- frame flicker effect added

-----

Building a game UI system for player stats

Must have: player health bar, gems collected, weapon (yes/no depending on storyline) / later boss health bar

Format similar to dialog box for style

Build a reusable gameUI class?

- added weapon property to game manager

- built a class to draw a panel using rect() (extra paramenters for rounded corners)

//AI used for information on how to build a moving health bar//
- "text(`${player.health} / ${player.maxHealth}`, x + 165, y + 54);" etc.

- boss panel method using the same style

- display the UI in respective scenes

- bug! UI was not anchored to the screen similar to the player - was being drawn inside the camera translated block - moved to draw after pop()

-----

- removed all debugging methods and drawings, final tests